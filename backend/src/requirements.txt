boto3>=1.34.0
# The Cedar Python binding is published as `cedarpy` — `cedarpolicy` does not
# exist on PyPI, so the original pin could never install.
cedarpy>=3.0.0
# Role 2's knowledge base and Open Food Facts client are imported in-process.
requests>=2.31
# Optional: only needed for AAHAR_MODEL_PROVIDER=groq (OpenAI-compatible API).
# openai>=1.0
# Label-photo OCR. Tesseract is the current stand-in — replace with AWS
# Textract (AAHAR_OCR=textract) once an AWS account is available.
pytesseract>=0.3.10
Pillow>=10.0
