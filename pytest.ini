[pytest]
# Role 2, Role 1 and the integration layer. Agent tests skip themselves when the
# Strands SDK is not installed.
testpaths = data/tests agent/tests backend/tests
pythonpath = . backend/src
addopts = -q
